# yaml-language-server: $schema=../../schema/server.schema.json
# Example: an MCP server requiring a bearer token. Users supply their own key.

name: Example GitHub
slug: example-github
description: >-
  Example MCP server requiring a personal access token. The marketplace never
  distributes credentials — installers replace the placeholder with their own.

url: https://github.example.com/mcp
transport: http

auth:
  type: bearer

tags:
  - example
  - developer-tools

homepage: https://github.com/harvard-huit/huit-plugin-marketplace
category: Developer Tools
version: 0.1.0
