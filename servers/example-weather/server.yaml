# yaml-language-server: $schema=../../schema/server.schema.json
# Example: a public, no-auth MCP server. Copy this directory as a starting point.

name: Example Weather
slug: example-weather
description: >-
  Example MCP server exposing current conditions and forecasts. Replace this
  entry with a real server, or delete it.

url: https://weather.example.com/mcp
transport: http

auth:
  type: none

tags:
  - example
  - weather

category: Productivity
version: 0.1.0
