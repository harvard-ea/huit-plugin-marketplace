{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/harvard-ea/plugin-marketplace-manager/schemas/codex-plugin.schema.json",
  "title": "servers/<slug>/.codex-plugin/plugin.json",
  "description": "mcpServers points at the plugin's MCP config for server entries. A skill-only plugin omits it and sets `skills` instead — Codex requires at least one component, not specifically an MCP server (verified against developers.openai.com/codex/plugins/build, 2026-08-18). No-auth/oauth entries share ./.mcp.json with Claude; credentialed entries use ./.codex-plugin/mcp.json because Codex reads http_headers, not Claude's headers key.",
  "type": "object",
  "required": [
    "name",
    "version",
    "description"
  ],
  "properties": {
    "name": {
      "type": "string",
      "pattern": "^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?$",
      "maxLength": 64
    },
    "version": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 500
    },
    "author": {
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "type": "string",
          "minLength": 1
        }
      }
    },
    "mcpServers": {
      "type": "string",
      "pattern": "^\\./(?!.*\\.\\.)[^\\s]+$"
    },
    "interface": {
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string"
        }
      }
    },
    "skills": {
      "type": "string",
      "pattern": "^\\./(?!.*\\.\\.)[^\\s]*$",
      "description": "Relative path to the bundled skills directory, e.g. \"./skills/\"."
    }
  }
}
